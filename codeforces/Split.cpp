#include <bits/stdc++.h>
using namespace std;

int main(){
  int t;
  cin >> t;

  vector<int> testcases(t);
  for (int i = 0; i < t; i++) {
    int n;
    cin >> n;
    vector<int>f(2*n);
    vector<int>fq(n);
    for (int j = 0; j < 2*n; j++) {
      cin >> f[i];
    }
  }
}
